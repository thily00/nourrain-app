export type User = {
    id: number;
    email: string;
    firstname: string;
    lastname: string;
    created_at: string;
    password: string;
    wallet: number;
};

export type Nourrain = {
    id: number;
    name: string;
    wallet: number;
};